import React from 'react';

const FindStore: React.FC = () => {
    return (
        <div className="find-store">
            <h1>Find a Store</h1>
            <p>Use this page to locate a store near you.</p>
        </div>
    );
};

export default FindStore;